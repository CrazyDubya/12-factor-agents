# API Key Security

**IMPORTANT:** A Google Maps API key was previously leaked in git history and has been removed.

If Google Maps functionality is needed, create a new API key with proper restrictions and add it to:
- Environment variable: `GOOGLE_MAPS_API_KEY`
- Or config file (ensure it's in .gitignore)
